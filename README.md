# NVGTPM Bucket Main
The main bucket of the [NVGTPM](https://github.com/harrymkt/nvgtpm) command line package manager.

To contribute, add your `<module>.json` in the json directory of this repository, or cd to the json directory and, if NVGTPM is installed and is in the path environment, type `nvgtpm create` and follow the prompts.
